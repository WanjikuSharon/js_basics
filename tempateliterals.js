firstName = 'John';
secondName = 'Gashia';

let fullname = `${firstName} ${secondName}`;//in one line

let fullName = `${firstName} 
${secondName}`;//in multi line

console.log(fullName);
console.log(fullname);